# Optional overrides — by default Vite reads repo-root .env and maps:
#   AUTH0_DOMAIN → VITE_AUTH0_DOMAIN
#   AUTH0_CLIENT_ID → VITE_AUTH0_CLIENT_ID
#   AUTH0_AUDIENCE → VITE_AUTH0_AUDIENCE
# Set those in the root .env (see demo/.env.example).
